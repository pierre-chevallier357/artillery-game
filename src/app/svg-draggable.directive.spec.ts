import { SvgDraggableDirective } from './svg-draggable.directive';

describe('SvgDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
