import { SvgZoomableDirective } from './svg-zoomable.directive';

describe('SvgZoomableDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgZoomableDirective();
    expect(directive).toBeTruthy();
  });
});
